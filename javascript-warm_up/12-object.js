#!/usr/bin/node

const myObject = {
    type: 'object',
    value: 12
  };

/* write a script to replace the value of 12 with 89 */

  console.log(myObject);
  myObject.value = 89;
  console.log(myObject);
  